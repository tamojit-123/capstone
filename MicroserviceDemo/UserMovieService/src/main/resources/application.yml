server:
  port: 8081
spring:
  data:
    mongodb:
      uri: ${MONGO_URI}
  application:
    name: user-movie-service
eureka:
  client:
    fetchRegistry: true
    registerWithEureka: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
